version: 0.2

batch:
  fast-fail: true
  build-order: SEQUENTIAL # 👈 ensures builds run one after another
  build-list:
    - identifier: create-projects
      buildspec: terraform_buildspec.yml
    - identifier: build_and_push_docker_to_ecr
      buildspec: buildspec.yml
